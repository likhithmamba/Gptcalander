import React from 'react';

/**
 * This view is obsolete under the new calendar-first architecture.
 * It is kept as an empty component to prevent build errors from old import paths.
 */
export const PressureView: React.FC = () => {
  return null;
};
